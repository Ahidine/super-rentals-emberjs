<div class="map">
les variables = {{this.variable}}
  <img
    alt="Map image at coordinates {{@lat}},{{@lng}}"
    ...attributes
    src="{{this.src}}"
    width={{@width}} height={{@height}}
  >

</div>